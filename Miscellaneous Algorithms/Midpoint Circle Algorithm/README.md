# Midpoint Circle Algorithm
Calculates the first octant of a circles using the given central X and Y coordinates and radius (all as integers).

## Usage:
Compile:

`gcc main.c -o main`

Run:

`./main`

## Output
It will output each point for the first octant using the Midpoint Circle Algorithm.
